<div class="container">
  <h2 class="text-center">Instruments</h2>
  <%= link_to "New Instrument", new_instrument_path, class: "button-green" %>
    <div class= "d-flex">
      <% @instruments.each do |instrument| %>
        <div class="card_instrument_index">
          <h6><%= link_to instrument.name, instrument_path(instrument)%></h6>
        </div>
      <% end %>
    </div>
  </div>
</div>

